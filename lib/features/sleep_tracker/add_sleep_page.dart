import 'package:flutter/material.dart';

class AddSleepPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Add Sleep Manually")),
    );
  }
}
